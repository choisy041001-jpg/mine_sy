# [MY인사] 모바일 — 연차 PRD

## 문서 정보

| 항목 | 내용 |
|------|------|
| 서비스명 | MY인사 |
| 메뉴 | 연차 (하단 탭 2번) |
| 화면 범위 | M-010, M-011, M-012, M-013, M-014, M-015, M-016, M-017 |
| 플랫폼 | 모바일 웹 (375 px) |
| 기준 문서 | `MY인사_PRD_v2.0.md` v2.5 (2026-05-26) |

> 연차 신청·취소의 실제 결재(승인·반려)는 경리나라 **결재함** 앱에서 처리됩니다. M-016·M-017은 결재 결과를 확인하는 읽기 전용 화면이며, 앱 푸시 알림 탭으로 직접 진입할 수 있습니다.

---

## 화면 목록

| 화면 ID | 화면명 | route |
|--------|-------|-------|
| M-010 | 연차 관리 | `leave-approval` |
| M-011 | 연차 신청 | `leave-apply` |
| M-012 | 연차 사용내역 | `leave-history` |
| M-013 | 연차사용계획서 | `leave-plan` |
| M-014 | 연차 취소 신청 | `leave-cancel` |
| M-015 | 휴가신청서 상세 — 진행중 | `leave-detail-wait` |
| M-016 | 휴가신청서 상세 — 승인 | `leave-detail-done` |
| M-017 | 휴가신청서 상세 — 반려 | `leave-detail-reject` |

---

## 화면별 명세

### M-010 연차 관리 (`leave-approval`)

| 항목 | 내용 |
|------|------|
| **진입 조건** | 하단 탭바 '연차' 탭 |
| **UI 요소** | 상단 타이틀 '연차 관리' + 부제('연차 신청 및 사용 내역') / 잔여 연차 현황 카드(잔여 일수 대형 숫자·총 연차·사용 일수·프로그레스 바) / 연차 사용내역 섹션(최근 3건 목록 + [전체 보기] 탭 → `leave-history`) / 결재 현황 카드 목록(휴가신청서/연차사용계획서 구분·날짜·종류·일수·상신일·결재자·상태 뱃지: 진행중/승인/반려) / 연차 FAB(+): 연차 신청·취소 신청·연차사용계획서 |
| **사용자 액션** | [전체 보기] 탭 → `leave-history` / 진행중 카드 탭 → `leave-detail-wait` / 승인 카드 탭 → `leave-detail-done` / 반려 카드 탭 → `leave-detail-reject` / FAB → 연차 신청 → `leave-apply` / FAB → 취소 신청 → `leave-cancel` / FAB → 연차사용계획서 → `leave-plan` |
| **다음 화면** | `leave-history` (M-012), `leave-detail-wait` (M-015), `leave-detail-done` (M-016), `leave-detail-reject` (M-017), `leave-apply` (M-011), `leave-cancel` (M-014), `leave-plan` (M-013) |

### M-012 연차 사용내역 (`leave-history`)

| 항목 | 내용 |
|------|------|
| **진입 조건** | `leave-approval` → 연차 사용내역 섹션 [전체 보기] 탭 |
| **UI 요소** | 상단 뒤로가기 + '연차 사용내역' / 필터 탭(전체·연차·반차) / 이력 목록(날짜 섹션 헤더 + 구분선형 행: 문서 구분·종류·일수·상신일·결재자·상태 뱃지·화살표) |
| **사용자 액션** | 필터 탭 전환 → 해당 종류만 표시 / 이력 행 탭 → 진행중: `leave-detail-wait` / 승인: `leave-detail-done` / 반려: `leave-detail-reject` |
| **다음 화면** | `leave-detail-wait` (M-015), `leave-detail-done` (M-016), `leave-detail-reject` (M-017) |

### M-011 연차 신청 (`leave-apply`)

| 항목 | 내용 |
|------|------|
| **진입 조건** | `leave-approval` FAB → 연차 신청 |
| **UI 요소** | 상단 뒤로가기 + '휴가신청서' / 기간 입력(시작일~종료일) / 캘린더(월 이동·날짜 드래그 범위 선택) / 휴가 종류 토글(연차/오전 반차/오후 반차) — **필수** / 사유 입력 텍스트에어리어 — **필수** / 결재선 표시(경리나라 자동 지정·신청자→결재자 / 결재선 2개 이상 시 [결재선 변경] 표시 — 탭 시 라디오 버튼 팝업으로 결재선 선택) / 신청하기 버튼 |
| **사용자 액션** | 날짜 드래그/탭 → 기간 선택 자동 입력 / 신청하기 탭 → 상신 후 `leave-approval` 복귀 / 잔여 연차 부족 시 인라인 알림 표시·신청 버튼 비활성화 |
| **다음 화면** | `leave-approval` (M-010) |

### M-013 연차사용계획서 (`leave-plan`)

| 항목 | 내용 |
|------|------|
| **진입 조건** | `leave-approval` FAB → 연차사용계획서 |
| **UI 요소** | 상단 뒤로가기 + '연차사용계획서' / 월 이동(‹ 2026년 5월 ›) / 캘린더(다중 날짜 선택·드래그 지원) / 선택된 날짜 수 + 잔여 연차 표시 / 계획 사유/메모(선택) / 결재선 표시(결재선 2개 이상 시 [결재선 변경] 표시 — 탭 시 라디오 버튼 팝업으로 결재선 선택) / 제출하기 버튼 |
| **사용자 액션** | 날짜 다중 선택 → 선택 일수 자동 카운트 / 제출하기 탭 → 상신 후 `leave-approval` 복귀 |
| **다음 화면** | `leave-approval` (M-010) |

### M-014 연차 취소 신청 (`leave-cancel`)

| 항목 | 내용 |
|------|------|
| **진입 조건** | `leave-approval` FAB → 취소 신청 |
| **UI 요소** | 상단 뒤로가기 + '연차 취소 신청' / 안내 배너(이미 승인된 연차 중 취소할 날짜 선택 안내) / 캘린더(승인된 연차 날짜 점 표시·드래그 범위 선택) / 취소할 휴가 종류 토글(연차/오전 반차/오후 반차) — **필수** / 취소 사유 선택(일정 변경/업무 일정/개인 사정/기타) — **필수** / 취소 사유 텍스트에어리어(추가 입력, 선택) / 취소 시 연차 현황 미리보기(선택 일수 반영) / 결재선 표시(결재선 2개 이상 시 [결재선 변경] 표시 — 탭 시 라디오 버튼 팝업으로 결재선 선택) / 상신하기 버튼 |
| **사용자 액션** | 날짜 탭/드래그 → 취소 대상 날짜 선택 / 상신하기 탭 → `leave-approval` 복귀 |
| **다음 화면** | `leave-approval` (M-010) |

### M-015 휴가신청서 상세 — 진행중 (`leave-detail-wait`)

| 항목 | 내용 |
|------|------|
| **진입 조건** | `leave-approval`에서 진행중 카드 탭 |
| **UI 요소** | 상단 뒤로가기 + '휴가신청서' / 진행중 뱃지 + '결재 진행중 / {결재자} 확인 전입니다' 안내 / 결재선 표시 / 신청 내용(날짜·종류·사유) |
| **사용자 액션** | 없음 (읽기 전용) |
| **다음 화면** | 없음 |

### M-016 휴가신청서 상세 — 승인 (`leave-detail-done`)

| 항목 | 내용 |
|------|------|
| **진입 조건** | `leave-approval`에서 승인 카드 탭, 또는 **앱 푸시 알림(승인) 탭** |
| **UI 요소** | 상단 뒤로가기 + '휴가신청서' / 승인 뱃지 + '{결재자}이 승인하였습니다' 안내 / 결재선(승인 완료 표시) / 신청 내용 |
| **사용자 액션** | 없음 (읽기 전용) |
| **다음 화면** | 없음 |

### M-017 휴가신청서 상세 — 반려 (`leave-detail-reject`)

| 항목 | 내용 |
|------|------|
| **진입 조건** | `leave-approval`에서 반려 카드 탭, 또는 **앱 푸시 알림(반려) 탭** |
| **UI 요소** | 상단 뒤로가기 + '휴가신청서' / 반려 뱃지 + '{결재자}이 반려하였습니다' 안내 / 반려 사유(결재자 메시지·처리 일시) / 결재선 표시 / 신청 내용(날짜·종류·사유) / 처리 이력 타임라인 |
| **사용자 액션** | 없음 (읽기 전용) |
| **다음 화면** | 없음 |

---

## 예외 처리

| 화면 ID | 에러 케이스 | 처리 방식 |
|--------|-----------|---------|
| M-011 | 날짜 미선택 | 신청하기 탭 시 날짜 선택 유도 인라인 메시지 |
| M-011 | 잔여 연차 부족 | '잔여 연차가 부족합니다' 인라인 알림, 신청 버튼 비활성화 |
| M-014 | 취소할 휴가 종류 미선택 | 필드 에러 스타일 표시 |
| M-014 | 취소 사유 미선택 | 필드 에러 스타일 표시 |

---

## 완성 기준

- [ ] 연차 관리 화면에서 잔여 연차 및 결재 현황(진행중/승인/반려) 카드 확인
- [ ] 캘린더 날짜 드래그로 연차 신청 및 결재 상신
- [ ] 연차 취소 신청 (날짜·휴가 종류·취소 사유 선택) 및 연차사용계획서 제출
- [ ] 알림 목록에서 알림 행 탭 시 연차 승인(M-016)/반려(M-017) 화면으로 딥링크 이동 확인
